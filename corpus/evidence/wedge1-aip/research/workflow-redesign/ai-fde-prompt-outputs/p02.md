## Context report (Rule 2)

| Field                | Value                                                                                                                            |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| Active project       | `CORDON` — :resource[ri.compass.main.folder.1767cec7-910c-476a-b472-4ec91bd9e6c3]                                                |
| Contents             | 2 children only: `cordon-reference-library`, `Planning` notepad — **clean, no build resources, no historical carry-over**        |
| Active branch        | **None attached.** Unbranched/main context → **not a write target**; this prompt performs no writes                              |
| Namespace / Ontology | `ri.compass.main.folder.fcea9c78-c4d4-424a-bc7f-cd817dbb8534` / `ri.ontology.main.ontology.cfb1478a-5b0e-466a-aaff-b212870e8861` |
| Historical project   | `ri.compass.main.folder.f9898a87-ea4e-404a-9414-039adda6fee1` — **not a target, read-only forever**                              |
| Ownership / review   | Owen owns and reviews; AI FDE authors under Owen's identity. Per your direction, no role/group design in this plan               |

Plan only. Zero writes performed.

---

# Prompt 02 plan — clean build home and Global Branch

## 1. Project and naming

**Project:** existing `CORDON` :resource[ri.compass.main.folder.1767cec7-910c-476a-b472-4ec91bd9e6c3]. No new project. It is empty of build resources, so it _is_ the clean room; the job is to keep it that way.

**Naming prefix — `cordon` / `Cordon`.** The ontology is shared with unrelated types, so every CORDON artifact is prefixed, and backstage types are visibly marked so they can never be mistaken for operator surface.

| Artifact                       | Convention                                                                 | Example                                                    |
| ------------------------------ | -------------------------------------------------------------------------- | ---------------------------------------------------------- |
| Visible object type API name   | `Cordon<Entity>`                                                           | `CordonIntervention`, `CordonCadastralParcel`              |
| Backstage object type API name | `CordonBs<Entity>`                                                         | `CordonBsInterventionOccurrence`, `CordonBsParcelStanding` |
| Link type ID                   | `cordon-<source>-<target>-<fact>`                                          | `cordon-pursuit-member-party`                              |
| Action API name                | `cordon-<verb-phrase>`                                                     | `cordon-dispatch-intervention`                             |
| Function API name              | `cordon<FunctionName>`                                                     | `cordonAssessNamedActionReadiness`                         |
| Dataset                        | `cordon_<layer>_<entity>` where layer ∈ `raw, cur, occ, link, idx, opt`    | `cordon_cur_cadastral_parcel`, `cordon_occ_intervention`   |
| Type groups                    | `cordon-operator` (11 visible), `cordon-backstage` (14 hidden + 4 `[LOG]`) | —                                                          |
| Branches                       | `ai-fde/owassmer1/cordon-w1-*`                                             | matches existing enrollment convention                     |

**Blocklist — names that must never appear:** `Eligibility assessment`, `Felling order`, `Recompute eligibility`, `Refresh official zone assignments`, `Evaluate potential impact`, `Ingest parsed felling order`, and the seven historical relation names. Any of these appearing in a diff is an automatic clean-room failure.

## 2. Global Branch

| Property                    | Value                                                                                                                                                                               |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Name                        | `ai-fde/owassmer1/cordon-w1-build`                                                                                                                                                  |
| Base                        | `main`                                                                                                                                                                              |
| Scope                       | Ontology (object/link/action types), Pipeline Builder, datasets, Workshop, AIP resources                                                                                            |
| **Explicitly out of scope** | **TypeScript v2 Functions repo** — TSv2 cannot join Global Branching (X-15). It uses local repo branches `cordon-w1/<work-package>`, and its **published tag** is the release event |
| Lifecycle                   | Opened at Prompt 02 execution; stays open across Prompts 03–13                                                                                                                      |
| Rebase                      | Rebase onto main before each prompt's evidence run; record the rebase in the ledger                                                                                                 |
| Proposal                    | One proposal per completed prompt as a review checkpoint — created only under that prompt's token, **never merged**                                                                 |
| Merge                       | **Prohibited until Prompt 14**, and then only with recorded semantic + release dispositions and Owen's token                                                                        |
| Per-Action branch setting   | Each of the four Actions needs `Allow functions with external calls when running this action on branches` = ON before it can be tested on the branch (X-16); record per Action      |
| Branch indexing             | Every branch test plan includes a wait-for-index step; the staged-write path reports missing index as an opaque 500 (X-17)                                                          |

**Do not reuse** the verification branch `ri.branch..branch.8e62f9e4-2d49-45b1-b5bf-14ce9d7c0ada` (sandbox, pending deletion). It is not the build branch.

## 3. Folder taxonomy inside CORDON

```
CORDON/
├── 00-governance/      RID ledger, prompt plans, dispositions, compatibility manifests
├── 01-integrations/    Data Connection sources, egress policies, listeners, streams
├── 02-data-raw/        immutable raw datasets, media sets, quarantine/rejected rows
├── 03-data-curated/    cur_ / occ_ / link_ / idx_ / opt_ datasets + expectations
├── 04-ontology/        11 visible types, 14 backstage types, links, type groups
├── 05-actions/         4 action types + 4 [LOG] types
├── 06-functions/       TSv2 repo (local branches only)
├── 07-aip/             chatbot, tool schemas, prompts, retrieval config
├── 08-evals-models/    Eval suites, trajectory sets, model assets
├── 09-health-lineage/  Data Health checks, monitoring views, observability config
├── 10-release/         DevOps packages, release channels
├── 11-evidence/        before/after inventories, run IDs, receipts, refusal cases
└── 12-surface/         Prompts 13–14 only; stays empty until then
cordon-reference-library   (existing, authorities, read-only)
Planning                   (existing notepad)
```

## 4. Project imports and services

| Needed                                         | Note                                                                                  |
| ---------------------------------------------- | ------------------------------------------------------------------------------------- |
| Standard transform / Funnel / webhook services | present at enrollment level                                                           |
| Ontology `owenwassmer Ontology`                | shared; isolation via prefix + type groups                                            |
| Language model functions                       | enrollment-level; pin versions in the manifest (several models sunset 2026-09 → 11)   |
| Network egress policies                        | **new** policies for CORDON sources; do not attach the historical `0719d86d-…` policy |
| **Zero imports from `CORDON-Wedge-1`**         | hard rule; the reference library is _read in place_, never imported                   |

## 5. RID ledger

One append-only ledger in `00-governance`. Every row, no name-only entries:

`prompt_id · resource_kind · display_name · full_rid · api_name · version_or_tag · parent_folder_rid · project_rid · global_branch_rid · ontology_branch_rid · code_repo_branch · backing_resources · created_by · created_at · approval_token · evidence_run_ids · semantic_disposition · release_disposition · status · rollback_target`

**Maintenance rule:** the ledger is appended _before_ a prompt is reported complete. A prompt with unledgered resources is incomplete by definition. Rows are never edited — corrections are new rows with a `supersedes` reference.

## 6. Clean-room checks

Run at Prompt 02 baseline (trivially passing on an empty project — that is the point: it establishes the baseline) and re-run at every prompt completion.

| #   | Check                                                                            | Method                            | Pass                                    |
| --- | -------------------------------------------------------------------------------- | --------------------------------- | --------------------------------------- |
| C1  | No CORDON resource has an ancestor in `f9898a87-…`                               | folder listing                    | all parents under `1767cec7-…`          |
| C2  | No quarantined ontology RID appears in the branch diff                           | branch diff vs §2 quarantine list | zero intersection                       |
| C3  | No blocklisted API name or display name                                          | naming scan of diff               | zero hits                               |
| C4  | No object type datasource resolves to a historical dataset                       | object type source mapping        | all datasources under `03-data-curated` |
| C5  | No repo `resources.json` references historical repos `b3a8ec5b-…` / `148c7d1f-…` | file read                         | zero references                         |
| C6  | Project imports contain no historical entries                                    | project imports read              | empty of historical                     |
| C7  | Branch diff shows only _created_ entities, never _edited_ historical RIDs        | branch diff                       | zero edits to pre-existing RIDs         |
| C8  | No dataset shares transaction lineage with the historical project                | Data Lineage                      | no shared ancestor                      |
| C9  | Type groups contain only new types                                               | type group read                   | membership matches ledger               |

## 7. Environment separation

Global Branching is **intra-environment isolation**, not release management — the two are complementary, not substitutes. Path: this project + branch = development. Test and production promotion happen later through DevOps packages, with the compatibility manifest as the promotion unit (ontology + dataset schemas + expectations + function tags + Action versions + chatbot tool schema + model version + Evals suite + Automate versions).

**Caveat carried forward:** DevOps/release management is the one architectural dependency still unevidenced in this enrollment. Prompt 12 or 14 must prove it before any promotion is promised. Until then, "rollback" means branch discard or manifest revert, not environment rollback.

## 8. Creation sequence (execute only under the Prompt 02 token)

| Step | Action                                                            | Evidence                                                                 |
| ---- | ----------------------------------------------------------------- | ------------------------------------------------------------------------ |
| S1   | Create the 13 folders in CORDON                                   | folder RIDs → ledger                                                     |
| S2   | Create Global Branch `ai-fde/owassmer1/cordon-w1-build` from main | global branch RID + ontology branch RID + code-repo branch name → ledger |
| S3   | Create type groups `cordon-operator`, `cordon-backstage`          | group RIDs → ledger                                                      |
| S4   | Record naming convention + blocklist in `00-governance`           | ledger row                                                               |
| S5   | Baseline clean-room checks C1–C9                                  | results file in `11-evidence`                                            |
| S6   | Before/after project inventory                                    | inventory diff: 2 children → 15                                          |
| S7   | **STOP** for Owen's review                                        | —                                                                        |

**Allowed writes at execution:** folders, one branch, two type groups, two governance documents. **Nothing else** — no datasets, no ontology entities, no repos, no imports, no permission changes, no historical-project access.

**Rollback:** delete the new empty folders, type groups and governance docs; archive the branch. All are empty at this stage, so rollback is total and lossless. Deletion only on Owen's explicit authorization.

## 9. Carried-forward constraints for later prompts

- **X-04 open decision:** BYOM has no marking support. Recommended path — hosted open-source weights (Llama/Nemotron, confirmed available) in production; fine-tuning as a later separately-governed upgrade. Blocks Contract 8 as written, not Prompts 03–09.
- Staged writes cannot be previewed → keep heavy logic in unit-tested pure helpers; the staged-write wrapper stays thin.
- Chatbot approval-before-execution defaults ON and **must be disabled** for the four Action tools per S0 §6.
- Capacity Portfolio Proposal expiry must sit below the scenario TTL.
- CORDON never merges scenarios, so **no fifth Action type** is created for scenario merge.
- Pre-Prompt-02 housekeeping: delete the four sandbox artifacts in `Learning owenwassmer` so no `zz-preflight-*` resource is ever confused with a CORDON one.

**Token gates recorded, not used:** Prompt 03 remains globally blocked until Owen sends `GATE 7 ACCEPTED FOR BUILD`; its plan-implementation token is `APPROVE P03 SCHEMA PLAN`.

---

**State** — Plan complete, zero writes. Project context `CORDON` `ri.compass.main.folder.1767cec7-910c-476a-b472-4ec91bd9e6c3`, unbranched, not a write target. Target project verified clean (2 non-build children); historical project untouched and not referenced as a target. No Gate 7 surface chosen.

**Next** — On your Prompt 02 execution token I run S1–S7: 13 folders, one Global Branch `ai-fde/owassmer1/cordon-w1-build`, two type groups, naming convention + blocklist, baseline clean-room evidence, full RID ledger rows, then STOP.

**Escalations** — ① **X-04** still needs your decision before any model work is planned. ② **DevOps/release management remains unevidenced** — do not promise environment promotion until Prompt 12/14 proves it. ③ Delete the sandbox leftovers (branch `ri.branch..branch.8e62f9e4-…`, repos `cafbb678-…` and `5da52005-…`, dataset `045e16f2-…`) before Prompt 02 executes, so the clean-room baseline is unambiguous.
